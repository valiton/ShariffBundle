<div class="shariff" data-backend-url="{{ path('valiton_shariff_get') }}" data-services="{{ shariffConfig.services | json_encode() | lower }}" data-lang="{{ locale }}"{% if url is defined %} data-url="{{ url }}"{% endif %}></div>